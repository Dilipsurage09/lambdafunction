{
  "name": "lambda-docker-nodejs",
  "version": "1.0.0",
  "description": "AWS Lambda function using Node.js, axios, and moment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "moment": "^2.29.4"
  }
}
